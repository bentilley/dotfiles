# LF configuration

#settings

set hidden

# mappings

map <c-e> $$EDITOR ~/.config/lf/lfrc
map <c-s> source ~/.config/lf/lfrc

map D delete
map Y $echo $f | pbcopy
map b !git blame "$f"
map o $echo $f
map t toggle
